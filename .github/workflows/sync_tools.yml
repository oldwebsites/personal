      - name: Generate README
        run: |
          # 获取当前时间（北京时间建议用 UTC+8 调整，如果服务器是 UTC）
          UP_TIME=$(date -u +'%Y-%m-%d %H:%M' -d '+8 hours')  # 强制 +8 小时 → 北京时间

          # 使用 echo 逐行写入，避免任何缩进/YAML 解析问题
          echo "# Auto Sync Tools" > README.md
          echo "" >> README.md
          echo "本仓库每天自动同步以下文件：" >> README.md
          echo "" >> README.md

          echo "## 📌 v2rayN 最新版" >> README.md
          echo "- 下载链接（推荐 ghproxy 加速）：**https://ghproxy.net/https://github.com/${R_NAME}/blob/main/v2rayN-latest.zip**" >> README.md
          echo "- 文件名：v2rayN-latest.zip" >> README.md
          echo "- 更新时间：\`${UP_TIME}\`" >> README.md
          echo "" >> README.md

          echo "## 📌 FreeNodes Base64 订阅" >> README.md
          echo "- 下载链接（推荐 ghproxy 加速）：**https://ghproxy.net/https://github.com/${R_NAME}/blob/main/base64.txt**" >> README.md
          echo "- 文件名：base64.txt" >> README.md
          echo "- 更新时间：\`${UP_TIME}\`" >> README.md
          echo "" >> README.md

          echo "---" >> README.md
          echo "" >> README.md

          echo "## 📘 自定义路由规则 (guize.json)" >> README.md
          echo "多种加速方式（任选其一即可）：" >> README.md
          echo "" >> README.md
          echo "- **GitHub Raw**（官方直链，适合科学上网环境）：" >> README.md
          echo "  https://raw.githubusercontent.com/${R_NAME}/main/guize.json" >> README.md
          echo "" >> README.md
          echo "- **ghproxy 加速**（国内推荐）：" >> README.md
          echo "  https://ghproxy.net/https://raw.githubusercontent.com/${R_NAME}/main/guize.json" >> README.md
          echo "" >> README.md
          echo "- **jsDelivr CDN**（速度较快，适合大部分场景）：" >> README.md
          echo "  https://cdn.jsdelivr.net/gh/${R_NAME}@main/guize.json" >> README.md
          echo "" >> README.md

          echo "---" >> README.md
          echo "自动同步由 GitHub Actions 每天定时驱动。" >> README.md
          echo "如有问题欢迎 issue 反馈～" >> README.md
